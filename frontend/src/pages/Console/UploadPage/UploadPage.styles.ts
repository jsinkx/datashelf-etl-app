import { Space } from 'antd'
import styled from 'styled-components'

import { Button } from '@ui/Button/Button'

export const UploadPageSpaceContainer = styled(Space)`
	max-width: 400;
	margin-top: 20px;
	flex-direction: column;
`

export const UploadRawDataButton = styled(Button)`
	margin-top: 10px;
`
